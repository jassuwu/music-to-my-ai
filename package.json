{
  "name": "plainsong",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "dev": "bun --watch scripts/build.ts",
    "build:harness": "bun run scripts/build-harness.ts",
    "dev:harness": "bun --watch scripts/build-harness.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/chrome": "^0.2.6"
  }
}
